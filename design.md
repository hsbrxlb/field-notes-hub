# Field Notes Hub 设计规范

## 定位

Hub 是工作档案和阅读工具。页面先呈现项目、内容或行动，装饰不得延迟阅读。整站采用一套稳定的深色实底、暖金强调和无阴影文字系统。

视觉方向：清楚的文字层级控制阅读顺序；细线与稳定间距形成节奏；强调色保持稀少。

## 内容预算

- 页面标题区最多包含标题和一句必要说明。说明必须补充标题无法表达的范围、时间、状态或行动。
- 标题、按钮和导航禁止使用“只……不……”“先……再……”“不是……而是……”“从……到……”等模板化推进句式。真实步骤、事实纠错、法律边界和原话引用可以豁免，但不得出现在装饰性标题里。
- 每个模块只承担一个任务。删掉标题后仍能完整复述的副标题通常应删除。
- 公开页不展示模型工作过程、审核轮次、机器 ID、内部安全测试、无行动价值的抓取计数或重复边界说明。
- 成果记录最多显示日期、标题、状态、一句结果和入口。项目、类型、用途、关联工作不能同时重复出现。

## 字体

- 使用系统无衬线字体，避免外链字体阻塞首屏。
- 桌面 H1：28–34px；手机 H1：24–28px。
- H2：20–26px；H3：15–18px。
- 正文：15–16px，行高 1.65–1.8。
- 辅助信息最小 12px。信息过多时删除或重排，不用更小字号塞进去。
- 全大写文字必须增加字距，并限制在平台名、短标签或必要状态。

## 尺度与网格

- 基础间距采用 4、8、12、16、24、32、48、64px 八档，并通过 `--space-1` 至 `--space-8` 复用。少数为字形对齐或固定控件服务的尺寸需要在组件旁说明用途。
- 侧栏 208px，顶栏 48px。标准内容宽度 1080px，宽内容 1180px，图片评审页 1400px。
- 标准页面左右内边距为 32px，手机为 18px。
- 桌面页面使用侧栏加单一内容列；宽内容可以使用两列或三列，但同一区块保持一套对齐线。

## 色彩与阴影

- 正文位于稳定实色内容面上。表格、状态和长文不得直接压在复杂图片或纹理上。
- 文字、按钮、标签、表格和导航禁止 `text-shadow`。
- 阴影用于悬浮对话框或键盘焦点。静态标签、标题、卡片和正文不使用阴影。
- 暖金色用于日期、链接或单一重点，不能在同一区块承担多个角色。
- 文本对实际背景达到 WCAG 4.5:1；大字和非文字控件达到 3:1。
- 状态需要文字，不能只靠颜色。

## 网格与组件

- 默认使用排版、细线和留白分组。卡片需要真实交互、独立对象或比较任务。
- 去掉背景、圆角、边框和阴影后不影响理解或操作的容器应改为普通布局。
- 圆角仅用于输入控件、状态和对话框，最大 4px。信息标签优先使用普通文字。
- 侧栏宽度、内容宽度、分隔线、状态样式和间距由全局 token 控制。页面 CSS 不新增无语义的颜色系统。

## 响应式

- 固定验收宽度：390、768、1024、1440px。
- 触控目标最小 44×44px。
- 手机端保持标题、第一条有效内容和主要入口在首屏附近；表格改为清楚的键值行。
- 禁止横向滚动、被截断标题和窄到逐字换行的数据列。
- 760px 以下启用手机导航；768px 仍作为平板验收点，检查侧栏与内容共同占用的宽度。

## 性能

- 常规页面不加载装饰背景图。
- 首屏业务图建议小于 600KB；非首屏图片使用 `loading="lazy"` 并写明宽高。
- 字体不通过 CSS `@import` 加载。
- 不为装饰引入 JavaScript、模糊滤镜或持续动画。

## 动效

- 交互反馈使用 160ms，导航使用 180ms，图片或内容显现最多 420ms。
- 动效需要解释状态、方向或连续性。装饰性漂浮、循环动画和滚动视差不进入工作档案。
- `prefers-reduced-motion: reduce` 下把过渡降到 0.01ms，并关闭平滑滚动。

## 发布前验收

- 文字：标题模板句式、重复副标题、解释性小字、模型自述和空泛结尾均清理。
- 代码：没有叙述型注释、隐藏 fallback、吞异常、重复 helper、无依据 TODO 和无必要抽象。
- 视觉：检查文字阴影、最小字号、触控尺寸、首屏密度、卡片必要性和设计 token。
- 浏览器：四个固定宽度均检查运行时错误、坏图、横向溢出和主要内容位置。
