# Field Notes Hub 设计规范

## 定位

Hub 是工作档案和阅读工具。页面先呈现项目、内容或行动，装饰不得延迟阅读。整站采用流体背景、稳定的深色阅读面、暖金强调和无阴影文字系统。

视觉方向：清楚的文字层级控制阅读顺序；细线与稳定间距形成节奏；强调色保持稀少。

`assets/background.png` 是 Hub 外壳的固定识别元素。全站减法、性能优化或去 AI 味整改不得删除、替换或隐藏它；可读性不足时调整遮罩和内容面的透明度。

## 内容预算

- 页面标题区最多包含标题和一句必要说明。说明必须补充标题无法表达的范围、时间、状态或行动。
- 标题、按钮和导航禁用反转式、伪递进式和步骤口号式标题。真实步骤、事实纠错、法律边界和原话引用可以豁免，但不得出现在装饰性标题里。具体句式由本机 anti-AI-slop 规则拦截。
- 每个模块只承担一个任务。删掉标题后仍能完整复述的副标题通常应删除。
- 公开页不展示模型工作过程、审核轮次、机器 ID、内部安全测试、无行动价值的抓取计数或重复边界说明。
- 工作台页面不显示论文式来源列表、规则执行自述、内部代码字段或通用方法论填充。来源保留在数据和维护记录中；真实风险、审批条件只在相关操作处说明一次，不重复铺状态、提示或边界文案。演示页呈现演示本身，不用“结果整理”等通用章节暗示已有成果。
- 中文自然、专业、直接。标题使用明确名称（如“方案要点”“品牌表达”），不用“这次一起定下来的事”等聊天过渡语或“品牌声音”等生硬直译。小字必须改变读者的操作或判断，否则删除；邮件正文用朴实友好的邀请语，不用销售口号。
- 成果记录最多显示日期、标题、状态、一句结果和入口。项目、类型、用途、关联工作不能同时重复出现。

### 页面内容与分类准入

- 每个模块必须展示具体作品、真实配置、已核实状态或用户要求的方案。将品牌名换掉仍可用于任意公司的通用建议，不作页面正文。
- 用户没有要求时间表、试验周期或执行节奏时，不自动补“前30天”“四周试验”“第几周做什么”、邀约数量或停试标准。需要计划时先依据任务授权和事实约束，不能用计划填空。
- Discord 子页仅展示未锁定、未隐藏且当前有用途的频道，以及重要的已核实设置。机器人仅在有实际使用证据时展示名称与用途；未使用或用途不明的安装清单不展示。配置放在频道结构下方，空配置区省略。权限审计、未知配置和完整频道清单留在内部证据。私人成员信息和内部截图不发布。
- 首页直接进入 Discord，不恢复“总览”“最近成果”或与侧栏重复的“工作入口”。
- “当前进展”栏目与内容已移除，不通过首页摘要、旧链接或更新脚本重新生成。
- “社媒内容自动化生产”先说明输入、AI 图文制作和人工确认后的用途，当前样稿放在流程下方；完整作品进入独立样稿页查看。研究与方案、网站与系统、工作方法不再设置分类入口；旧链接定向转到已有的具体内容页。
- 删除不能改变判断或操作的“已有内容与方案”“相关运营专题”等重复跳转行。相关页面优先通过共享导航进入。
- 品牌普通文本统一为 OEDRO，与正式标识一致；用户指定的导航名称 Discord频道设计、Oedro persona、Oedro周边，以及 Oedro Buddy 专名保留原大小写。讨论入口使用“OEDRO讨论全网捕捉”。原话、账号名、URL 和正式资源不得机械替换。“产品与购买信息”优于“成交内容”等生硬标签。

## 字体

- 使用系统无衬线字体，避免外链字体阻塞首屏。
- 侧栏导航与品牌标题：13px（原 15px 减少 2px），由 `--type-nav` 控制；导航点击高度仍为 44px。
- Hub 各子页字号较原值减少 1px：桌面 H1 通常 27–33px；手机 H1 通常 26px，产品页 28px、内容页 27px。
- H2：19–25px；H3：14–17px。页面中的较小分节标题沿用自身层级。
- 正文：14–15px，行高 1.65–1.8。共享字号由 `--type-body`、`--type-small`、`--type-h1`、`--type-h1-mobile`、`--type-h2` 控制，组件的显式字号同步减少；流动标题的上下限及中间值均减少 1px。
- 12px 辅助信息保持原大小；13px 辅助信息减为 12px。嵌入邮件、图片内文字、独立样站、翻页书和调研应用使用各自设计系统，不跟随 Hub 外壳缩小。
- 辅助信息最小 12px。信息过多时删除或重排，不用更小字号塞进去。
- 全大写文字必须增加字距，并限制在平台名、短标签或必要状态。

## 尺度与网格

- 基础间距采用 4、8、12、16、24、32、48、64px 八档，并通过 `--space-1` 至 `--space-8` 复用。少数为字形对齐或固定控件服务的尺寸需要在组件旁说明用途。
- 侧栏 208px，顶栏 48px。标准内容宽度上限 1350px，宽内容 1475px，图片评审页 1750px（较原上限增加 25%，窄屏受可用宽度限制）。
- 标准页面左右内边距为 32px，手机为 18px。
- 桌面页面使用侧栏加单一内容列。只有需要并排比较的同类信息可分列；调研预览、邮件预览和周边概念图各自占满所在内容列，不与说明文字争宽度。同一区块保持一套对齐线。

## 色彩与阴影

- 正文底板使用 `--content-surface` 的 40% 深色遮罩，减少黑色实块，让流体背景贯穿页面。宽度和遮罩统一由全站 token 控制，避免局部旧 CSS 覆盖。以原 55% 遮罩减少约 30% 为起点，最终以真实截图和可读性验收为准；不以死守百分比牺牲文字对比度。长段落可限制行长，图片、表格和频道结构利用完整横向空间。
- 文字、按钮、标签、表格和导航禁止 `text-shadow`。
- 阴影用于悬浮对话框或键盘焦点。静态标签、标题、卡片和正文不使用阴影。
- 暖金色用于日期、链接或单一重点，不能在同一区块承担多个角色。
- 文本对实际背景达到 WCAG 4.5:1；大字和非文字控件达到 3:1。
- 状态需要文字，不能只靠颜色。

## 网格与组件

- 首批触达名单为工具页：沿用黑金外壳，首屏直接显示分类、人数、翻页与表格；每页100人，无搜索。全部名单按客户编号去重，高消费与近期访问可同时标记；分类切换回到第一页，未订阅与退订始终暂不邀请。完整表格占满可用宽度，手机仅表格内部横向滚动，表头固定；不增加装饰动画。人数入口用暖金色、下划线和斜箭头明确可点击。

- 默认使用排版、细线和留白分组。卡片需要真实交互、独立对象或比较任务。
- 去掉背景、圆角、边框和阴影后不影响理解或操作的容器应改为普通布局。
- 圆角仅用于输入控件、状态和对话框，最大 4px。信息标签优先使用普通文字。
- 侧栏宽度、内容宽度、分隔线、状态样式和间距由全局 token 控制。页面 CSS 不新增无语义的颜色系统。
- 普通资料页不设置右侧“本页内容”目录。确有多角色浏览任务的图片评审页可使用下述角色导航，不把这种结构复制到短文和企划页。

### 品牌标识与界面预览

- OEDRO 标识使用官网确认的正式资源，优先透明底高清原文件；保持比例、字形和完整轮廓，不以普通文字或 AI 猜测的字标替代。邮件、调研应用和周边效果图均使用同一品牌标识。
- AI 可生成周边场景，但品牌标识必须以正式资源为依据并逐图核对，不把近似拼写、变形标识当作设计方案。
- 界面预览来自实际应用截图。应用修改后重新截图并更新 Hub，不能通过图片编辑伪造尚未实现的界面；发布后核对线上图片，必要时更新资源版本避免旧缓存。

### 图片评审页

- 主要任务是按角色看清全部作品，不提供轮次、配色或状态筛选。
- 每个角色集中在一个角色区；跨批次生成的图片放在一起，来源批次保留在数据中，页面不显示。
- 合成总览不能替代独立图片。吉祥物概念图默认使用桌面两列、平板和手机单列，每张图片拥有完整标题与画面说明；周边企划采用下述全宽纵向排布。
- 图片说明描述画面里真正发生的事，避开“身份、结构、工作、动态”等制作阶段标签。
- 品牌字标位置测试保留无字原图，并把字标版作为独立图片展示；不在页面层临时覆盖文字。
- 页面不显示候选、淘汰或最终选择标签；选择结论不进入历史图片列表。
- 所有轮次默认完整展开，不使用折叠、加号、弹窗或图片灯箱。
- 角色页内导航在宽屏占据独立栏位，窄屏改为横向锚点栏；导航不得遮挡角色标题或图片。
- 页面不显示日期、生成目标或设计过程旁白。角色介绍最多一到两句，直接说明形象和寓意。
- 图片保持原始纵横比和完整画面，不裁掉耳、尾、翼、蹄、爪或工具接触点。

### 社媒内容自动化生产

- `content-studio.html` 先呈现“一个选题 → 三平台 AI 图文 → 人工确认后使用”的流程；平台为 Instagram、X、YouTube 社区，说明配图、英文文案和中文对照的交付关系。
- 流程使用原生 HTML / SVG，复用暖金、深色阅读面与系统字体，宽屏横向、窄屏纵向，不以装饰图替代流程含义。输入与交付各用一段说明，页面不伪造在线生成或发布按钮。
- 样稿作为下方实例，保留完整图片与英文配文，并提供完整样稿入口；现有案例不能暗示自动管线已通过或作品已发布。

### 完整样稿页

- 页名为“社媒内容制作（测试版）”。记录头不显示日期或版本标签；移除平台目录，按平台连续阅读。内容底色复用全站 40% 深色遮罩，内层使用 20% 遮罩，让流体背景透出。
- 当前任务是连续阅读作品：Prompt、简短目的、Instagram、X 和 YouTube 社区图文；中文对照默认展开。
- 仅展示 active_run_id 指定的当前记录。历史数据与原图保留，不把退回的作品继续铺在本页。
- 不显示平台解释副标题、图下注释、AI 审核、修改记录、评审问题或产品来源区；来源与审核仍参与数据校验，所有公开 JSON 仍需通过公开安全检查。
- Instagram 使用 4:5 图片，X 与 YouTube 社区图文可共用 1:1 图片；不为了平台数量重复制作素材。当前不制作或展示 TikTok、YouTube Shorts、Facebook、Pinterest。
- 保持完整图片和默认展开的双语段落，不用裁切、隐藏正文或增加装饰来填补空间。

### OEDRO讨论全网捕捉

- 每个原帖是一个独立阅读对象，以主题、社区、平台和原帖链接构成卡片。桌面三列、中屏两列、手机单列，完整标题可换行，原帖入口至少44px高。
- 暖金顶线与深色阅读面区分讨论对象，不显示抓取健康、机器状态和通用处理方法。已批准洞察存在时才显示对应区域；无记录时给出明确空状态。
- 卡片标题依据真实原帖主题，未知主题使用中性名称，不把一般讨论猜成投诉。数据中的来源、核实与安全字段保留，页面减法不删除证据。

### 导航与调研入口

- 调研相关页面属于 utility：一个“AI问卷”侧栏入口，完整界面预览占满内容宽度，说明与“开始AI调研问卷”按钮在图下。沿用系统字体、暖金按钮、44px 操作高度和全局间距。
- SEO、Discord、品牌表达各自进入原有资料页；页面标题与侧栏名称一致。旧资料索引保留网址，不占侧栏；旧公开信号专题跳到“问题与反馈”的处理方法区。
- 调研演示入口不铺准备、结果整理或参与说明章节；旧调研网址共用相同渲染内容。实际调研的参与规则属于应用与操作流程，不当作演示页填充。
- 侧栏顺序统一由 `data/content.json` 维护；“用户数据分析”紧挨“首批触达用户”之前，“社媒内容自动化生产”沿用原内容入口。样站外链保留醒目标识。重要工作变化时调整共享导航，不在各页各排一套。
- 吉祥物设计的skill 直接提供完整可复制、可下载的通用工作包；不展示本人的设计过程，不依赖本机路径、私有 Skill 或特定品牌。SEO 正文与步骤为 15px、使用亮色；下一步按上下结构排列，正文不出现人名。
- 独立站入口用暖金文字与 ↗，沿用当前窗口跳转，附离开工作台进入样站的无障碍提示；维护进度标签从 Hub 列表移除，底层状态和安全事实仍保留。

### AI 调研应用

- 应用沿用自身浅色视觉，不套用 Hub 的深色外壳。页眉使用正式 Logo，左边缘对齐问题正文；“AI Research for Drivers”位于同一页眉行右侧，不再使用“Light Research”。窄屏缩放与换行不得破坏阅读或造成横向溢出。
- 进度说明单独一行，题号和进度线共同占用下一行的完整宽度；不让进度条与说明文字互相挤压。
- 不显示 Skip question、Options 或原跳题提示句，不提供跳题入口；保留结束访谈及结束后的必要数据操作。界面精简不能靠隐藏仍可触发的跳题行为实现。

### 用户邮件模板

- 页面属于 reference：邮件预览独占完整一行，正文说明位于预览下方；不设置抢占预览宽度的右栏。技术导入文件可留在项目中，页面不重复展示下载按钮或代码字段。
- 延用 Hub 标题 27–33px、正文 14–15px、辅助 12px 起、暖金链接、既有间距与 44px 操作目标。无新背景、卡片圆角或阴影；交互只用共享导航反馈。
- 邮件为独立 600px 表格和内联样式，使用适合品牌与阅读的浅色正文面，非强制纯白。官网蓝色 Logo、图片、正文和按钮共用左右对齐线；移动端左右内边距 24px。邮件强调色与 Hub 暖金导航分属不同表面。
- 场景图按 2:1 完整显示；仅为概念场景，不作为产品事实或用户照片。邮件图片使用 HTTPS，Hub 预览加载对应本地资源；目标小于 1MB，文字与 CTA 不烘焙进图片。
- 邀请邮件只有一个主要动作；参与可以是发照片、问短问题或浏览，不要求讲故事或写长文。未批准的优惠方案、方括号占位正文和优惠计算不进入公开预览；原稿保留在内部或版本历史。真实发送文件保留公司地址及退订绑定，预览不伪造地址。模板可查看不代表获准发送。

### 产品知识库

- 页面属于 reference / utility 混合界面。视觉方向是“暖黑半透明资料台 + 严格网格”：商品像有秩序的零件目录，暖金只标识当前分类、价格和可行动链接。
- 首屏直接交代官网快照日期、产品总量、品牌与分类覆盖，并展示分类矩阵和当前分类的第一批商品；不设置搜索框，不用装饰性大图推迟内容。
- 分类矩阵负责全库导航，品牌与资料状态只作当前分类内的少量筛选。切换分类后只载入对应数据文件；商品每页 24 项，避免把 1,000 多个商品同时写入 DOM。
- 商品卡片默认展示官网主图、完整商品名、SKU、车型适配、价格快照、库存文字与评价。完整说明、卖点、规格、包装、文档、变体、质保口径和全部媒体在原生 details 中按需展开。
- 价格、库存、评分和适配均标明为官网快照；出现官网口径冲突、无适配段落、无图片或无效说明书时直接标出“需复核”，不替官网猜答案。商品名称和官网链接始终保留英文原文。
- 桌面用三列等宽网格，平板两列，手机单列。任何规格和值都允许换行；表格改为标签和值的纵向定义列表，页面不得产生横向长滚动。
- 图片使用官网 HTTPS 地址、固定宽高和 lazy loading；首个可见商品图优先加载。详情展开只渲染当前商品内容，分页和筛选结果通过 `role=status` 告知辅助技术。

### 可见正文硬性禁入

- 所有 Codex、AI 和维护者发布前必须移除：模型思考或推演、审核与生成过程、无必要的 AI 图片声明、模板占位字符、无用状态说明、未采用平台与无实际内容的栏目。不能把这些段落改名为“说明”“提示”后保留。
- 内容是否该出现由读者用途决定。法律要求、真实风险和操作必需的信息不得因减法删除；放在相关动作处，具体且仅出现一次。素材来源与权利判断仍保留在内部，不将示意图当作真实用户证言。
- 静态扫描必须覆盖邮件 HTML 和页面渲染源码，并增加被退回文案的回归检查；代码通过后仍要看实际预览。规则存在和扫描通过均不等于文案可用。
- OEDRO 社媒优先挖掘车主共有痛点与轻松日常；明确谁发起内容。官方选题不等于自然投稿，真实用户分享需要自愿动机与使用授权，不虚构使用时长或测试结论。

### 品牌与周边企划

- 两条路线均为讨论用 reference 阅读页。首屏呈现标题、核心建议和方案要点，战略图接续正文，不使用全屏装饰首图，也不把建议写成已确认的会议决定。
- 复用系统字体、暖金强调和全局字号、间距；正文 15px、行高至少 1.65，不另设更大的标题上限。
- 内容复用全站 40% 深色遮罩，嵌套面板使用 20% 遮罩；连续阅读为主，仅真实对照内容可双栏，不用重复卡片堆砌。周边三个设计方向按“标题、图、必要说明”逐项纵向排列，每张图独占完整内容宽度，桌面也不并排缩成三张小图。
- 桌面流程可横向，窄屏改纵向，所有节点保留。概念图完整展示、无裁切，不加重复的“概念效果图”图注；方案与实物参考仍需区分。周边既是活动载体，也是居家与出门时的日常品牌接触；社媒配图只解释这一关系。触控最小 44px，手机只保留单列阅读。
- 同行周边、采购候选采用纵向图文布局。供应商样式图不能冒充 OEDRO 已收到的样品；平台标价、待复核调研记录价与规划预留分别标明，质量结论以实物验收为准。社媒帖子示例不得伪装为真实用户互动。
- 无新动画、无新依赖；现有菜单交互与 reduced-motion 保持。原生概念图留在本地，网页导出优先控制在每张 600KB 内。

### 首批触达与 Discord 邀请方案

- 两页均为 reference，复用企划页的连续单列阅读面、系统字体、全局色彩与间距，不增加依赖或装饰动效。首屏直接呈现标题和筛选结论或活动要点；明细在后续纵向章节展开。
- 首批触达主页承载筛选条件与汇总；名单子页仅发布 Oliver 明确授权的近期用户与高消费客户的客户编号、打码邮箱、历史订单、订阅状态和分类，不公开完整邮箱、原始导出或后台截图。邀请方案按邀请对象、参与方式、激励条件与入群承接组织；不能把历史优惠写成当前活动承诺。
- 专属样式限于 `outreach-plan.css`，表格式定义列表在手机改为单列；导航继续使用共享菜单的焦点管理与 reduced-motion 设置，无额外动效。

## 响应式

- 固定验收宽度：390、768、1024、1440px。
- 触控目标最小 44×44px。
- 手机端保持标题、第一条有效内容和主要入口在首屏附近；表格改为清楚的键值行。
- 禁止横向滚动、被截断标题和窄到逐字换行的数据列。
- 980px 以下使用收起式导航及键值行表格，避免平板五列文本逐字换行。关闭侧栏时不参与键盘焦点；打开后焦点留在导航，Escape 或关闭按钮返回菜单按钮。

## 性能

- 常规页面共享 Hub 固定流体背景，不再加载其他全页装饰背景图。
- Hub 固定流体背景是已批准的体积例外；其他首屏业务图建议小于 600KB，非首屏图片使用 `loading="lazy"` 并写明宽高。
- 字体不通过 CSS `@import` 加载。
- 不为装饰引入 JavaScript、模糊滤镜或持续动画。

## 动效

- 交互反馈使用 160ms，导航使用 180ms，图片或内容显现最多 420ms。
- 动效需要解释状态、方向或连续性。装饰性漂浮、循环动画和滚动视差不进入工作档案。
- `prefers-reduced-motion: reduce` 下把过渡降到 0.01ms，并关闭平滑滚动。

## 发布前验收

- 文字：标题模板句式、重复副标题、解释性小字、模型自述和空泛结尾均清理。
- 代码：没有叙述型注释、隐藏 fallback、吞异常、重复 helper、无依据 TODO 和无必要抽象。
- 视觉：检查文字阴影、最小字号、触控尺寸、首屏密度、卡片必要性和设计 token；核对正式 Logo、全宽图片、应用页眉与问题对齐、进度两行结构，以及预览与真实应用一致。
- 浏览器：四个固定宽度均检查运行时错误、坏图、横向溢出和主要内容位置。


## 用户数据分析

- utility分析页，入口位于首批触达用户之前；首屏直接提供全体客户/首批触达范围切换、真实人数与客户结构图。
- 系统字体，标题33px/手机26px，正文14–16px、辅助文字最小12px、统计数字等宽。全站外壳不变，分析面采用深色中性底；类别使用可区分的蓝、青绿、杏、紫、珊瑚色，圆环与图例保持对应，禁止再次用相近土黄色区分各组。
- 网格和细线组织客户结构、购买频次、累计金额、订阅与首批来源关系；全体与择选样本始终分别标明分母。金额为历史累计、不称净收入，无日期字段不绘制时间趋势。
- 客户结构图选择与范围切换有短过渡和键盘反馈；reduced-motion关闭动画。按钮44px，390/768/1024/1440逐档验收；窄屏纵向呈现，SVG标签不能随viewBox缩到12px以下。
- 复用现有流体背景，图表阅读面与网格保证对比度；不引入字体、图表依赖和外部分析请求。新增 JSON 只保留人数聚合，不含原始客户明细或财务总额。
- 大小悬殊的购买分组分区呈现，清楚标各区人数刻度；固定大小端点只作定位标记，位置仍使用真实比例。金额改用共同对数人数刻度点图，标注每格十倍，避免小人数柱压为细线；不能悄悄抬高最小柱长。
- 邮件订阅使用同心弧线与旁侧人数图例，不再用单根横向比例带。人数下方固定显示百分比，所有分组统一，不随数字长度换位置。全体与首批两个范围采用相同规则并分别验收。
- 删除标题下快照句及订阅发送提醒；必要统计口径折叠，首屏保留实际图表。交互只保留范围切换及能突出对应组的操作，不用按钮重复打印已展示数字。
