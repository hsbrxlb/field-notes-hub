diff --git a/src/BookShell.tsx b/src/BookShell.tsx
--- a/src/BookShell.tsx
+++ b/src/BookShell.tsx
@@ -66,7 +66,12 @@ export function BookShell({ title, children, onExit, suspended = false }: {
     } catch {
       setStatus("failed");
     }
+    let measuredWidth = mount.getBoundingClientRect().width;
     const observer = new ResizeObserver(() => {
+      const width = mount.getBoundingClientRect().width;
+      // Becoming visible changes height, not page geometry; do not cancel the first user turn.
+      if (Math.abs(width - measuredWidth) < .5) return;
+      measuredWidth = width;
       cancelAnimationFrame(resizeFrame);
       resizeFrame = requestAnimationFrame(() => {
         if (!disposed && engine.getPageCollection()) {
