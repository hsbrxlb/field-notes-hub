# Field Notes Hub 设计规范

## 定位

Hub 是工作档案和阅读工具。页面先呈现项目、内容或行动，装饰不得延迟阅读。整站采用流体背景、稳定的深色阅读面、暖金强调和无阴影文字系统。

视觉方向：清楚的文字层级控制阅读顺序；细线与稳定间距形成节奏；强调色保持稀少。

`assets/background.png` 是 Hub 外壳的固定识别元素。全站减法、性能优化或去 AI 味整改不得删除、替换或隐藏它；可读性不足时调整遮罩和内容面的透明度。

## 内容预算

- 页面标题区最多包含标题和一句必要说明。说明必须补充标题无法表达的范围、时间、状态或行动。
- 标题、按钮和导航禁用反转式、伪递进式和步骤口号式标题。真实步骤、事实纠错、法律边界和原话引用可以豁免，但不得出现在装饰性标题里。具体句式由本机 anti-AI-slop 规则拦截。
- 每个模块只承担一个任务。删掉标题后仍能完整复述的副标题通常应删除。
- 公开页不展示模型工作过程、审核轮次、机器 ID、内部安全测试、无行动价值的抓取计数或重复边界说明。
- 工作台页面不显示论文式来源列表、规则执行自述、内部代码字段或通用方法论填充。来源保留在数据和维护记录中；真实风险、审批条件只在相关操作处说明一次，不重复铺状态、提示或边界文案。演示页呈现演示本身，不用“结果整理”等通用章节暗示已有成果。
- 中文自然、专业、直接。标题使用明确名称（如“方案要点”“品牌表达”），不用“这次一起定下来的事”等聊天过渡语或“品牌声音”等生硬直译。小字必须改变读者的操作或判断，否则删除；邮件正文用朴实友好的邀请语，不用销售口号。
- 成果记录最多显示日期、标题、状态、一句结果和入口。项目、类型、用途、关联工作不能同时重复出现。

## 字体

- 使用系统无衬线字体，避免外链字体阻塞首屏。
- 桌面 H1：28–34px；手机 H1：24–28px。
- H2：20–26px；H3：15–18px。
- 正文：15–16px，行高 1.65–1.8。
- 辅助信息最小 12px。信息过多时删除或重排，不用更小字号塞进去。
- 全大写文字必须增加字距，并限制在平台名、短标签或必要状态。

## 尺度与网格

- 基础间距采用 4、8、12、16、24、32、48、64px 八档，并通过 `--space-1` 至 `--space-8` 复用。少数为字形对齐或固定控件服务的尺寸需要在组件旁说明用途。
- 侧栏 208px，顶栏 48px。标准内容宽度 1080px，宽内容 1180px，图片评审页 1400px。
- 标准页面左右内边距为 32px，手机为 18px。
- 桌面页面使用侧栏加单一内容列。只有需要并排比较的同类信息可分列；调研预览、邮件预览和周边概念图各自占满所在内容列，不与说明文字争宽度。同一区块保持一套对齐线。

## 色彩与阴影

- 正文位于稳定的深色半透明内容面上。流体背景可以透出，但表格、状态和长文不得直接压在未经遮罩的复杂图像上。
- 文字、按钮、标签、表格和导航禁止 `text-shadow`。
- 阴影用于悬浮对话框或键盘焦点。静态标签、标题、卡片和正文不使用阴影。
- 暖金色用于日期、链接或单一重点，不能在同一区块承担多个角色。
- 文本对实际背景达到 WCAG 4.5:1；大字和非文字控件达到 3:1。
- 状态需要文字，不能只靠颜色。

## 网格与组件

- 默认使用排版、细线和留白分组。卡片需要真实交互、独立对象或比较任务。
- 去掉背景、圆角、边框和阴影后不影响理解或操作的容器应改为普通布局。
- 圆角仅用于输入控件、状态和对话框，最大 4px。信息标签优先使用普通文字。
- 侧栏宽度、内容宽度、分隔线、状态样式和间距由全局 token 控制。页面 CSS 不新增无语义的颜色系统。
- 普通资料页不设置右侧“本页内容”目录。确有多角色浏览任务的图片评审页可使用下述角色导航，不把这种结构复制到短文和企划页。

### 品牌标识与界面预览

- OEDRO 标识使用官网确认的正式资源，优先透明底高清原文件；保持比例、字形和完整轮廓，不以普通文字或 AI 猜测的字标替代。邮件、调研应用和周边效果图均使用同一品牌标识。
- AI 可生成周边场景，但品牌标识必须以正式资源为依据并逐图核对，不把近似拼写、变形标识当作设计方案。
- 界面预览来自实际应用截图。应用修改后重新截图并更新 Hub，不能通过图片编辑伪造尚未实现的界面；发布后核对线上图片，必要时更新资源版本避免旧缓存。

### 图片评审页

- 主要任务是按角色看清全部作品，不提供轮次、配色或状态筛选。
- 每个角色集中在一个角色区；跨批次生成的图片放在一起，来源批次保留在数据中，页面不显示。
- 合成总览不能替代独立图片。吉祥物概念图默认使用桌面两列、平板和手机单列，每张图片拥有完整标题与画面说明；周边企划采用下述全宽纵向排布。
- 图片说明描述画面里真正发生的事，避开“身份、结构、工作、动态”等制作阶段标签。
- 品牌字标位置测试保留无字原图，并把字标版作为独立图片展示；不在页面层临时覆盖文字。
- 页面不显示候选、淘汰或最终选择标签；选择结论不进入历史图片列表。
- 所有轮次默认完整展开，不使用折叠、加号、弹窗或图片灯箱。
- 角色页内导航在宽屏占据独立栏位，窄屏改为横向锚点栏；导航不得遮挡角色标题或图片。
- 页面不显示日期、生成目标或设计过程旁白。角色介绍最多一到两句，直接说明形象和寓意。
- 图片保持原始纵横比和完整画面，不裁掉耳、尾、翼、蹄、爪或工具接触点。

### 内容生产评审页

- 页名为“社媒内容制作（测试版）”。记录头不显示日期或版本标签；移除平台目录，按平台连续阅读。内容底色使用 55% 深色遮罩，内层使用 28% 遮罩，让流体背景透出。
- 当前任务是连续阅读作品：Prompt、简短目的、Instagram、X 和 YouTube 社区图文；中文对照默认展开。
- 仅展示 active_run_id 指定的当前记录。历史数据与原图保留，不把退回的作品继续铺在本页。
- 不显示平台解释副标题、图下注释、AI 审核、修改记录、评审问题或产品来源区；来源与审核仍参与数据校验，所有公开 JSON 仍需通过公开安全检查。
- Instagram 使用 4:5 图片，X 与 YouTube 社区图文可共用 1:1 图片；不为了平台数量重复制作素材。当前不制作或展示 TikTok、YouTube Shorts、Facebook、Pinterest。
- 保持完整图片和默认展开的双语段落，不用裁切、隐藏正文或增加装饰来填补空间。

### 导航与调研入口

- 调研相关页面属于 utility：一个“AI智能用户调研”侧栏入口，完整界面预览占满内容宽度，说明与“开始AI调研问卷”按钮在图下。沿用系统字体、暖金按钮、44px 操作高度和全局间距。
- SEO、Discord、品牌表达各自进入原有资料页；页面标题与侧栏名称一致。旧资料索引保留网址，不占侧栏；旧公开信号专题跳到“问题与反馈”的处理方法区。
- 调研演示入口不铺准备、结果整理或参与说明章节；旧调研网址共用相同渲染内容。实际调研的参与规则属于应用与操作流程，不当作演示页填充。
- 侧栏按近期工作优先级排列：总览、当前进展、Discord、邮件、AI调研、社媒品牌、内容、周边、SEO、问题反馈、品牌表达；吉祥物、翻页演示、方法放后。样站外链作为独立入口保留醒目标识。重要工作变化时调整共享导航，不在各页各排一套。
- 独立站入口用暖金文字与 ↗，沿用当前窗口跳转，附离开工作台进入样站的无障碍提示；维护进度标签从 Hub 列表移除，底层状态和安全事实仍保留。

### AI 调研应用

- 应用沿用自身浅色视觉，不套用 Hub 的深色外壳。页眉使用正式 Logo，左边缘对齐问题正文；“AI Research for Drivers”位于同一页眉行右侧，不再使用“Light Research”。窄屏缩放与换行不得破坏阅读或造成横向溢出。
- 进度说明单独一行，题号和进度线共同占用下一行的完整宽度；不让进度条与说明文字互相挤压。
- 不显示 Skip question、Options 或原跳题提示句，不提供跳题入口；保留结束访谈及结束后的必要数据操作。界面精简不能靠隐藏仍可触发的跳题行为实现。

### 用户邮件模板

- 页面属于 reference：邮件预览独占完整一行，正文说明位于预览下方；不设置抢占预览宽度的右栏。技术导入文件可留在项目中，页面不重复展示下载按钮或代码字段。
- 延用 Hub 标题 28–34px、正文 15–16px、辅助 12px 起、暖金链接、既有间距与 44px 操作目标。无新背景、卡片圆角或阴影；交互只用共享导航反馈。
- 邮件为独立 600px 表格、内联样式的暖白阅读面：浅色页眉使用官网蓝色 Logo、衬线 40px 标题、16px 正文、14px 操作说明、12px 页脚，移动标题降至 34px、左右内边距 24px。邮件强调色用于主按钮，与 Hub 暖金导航分属不同表面。
- 场景图按 2:1 完整显示；仅为概念场景，不作为产品事实或用户照片。邮件图片使用 HTTPS，Hub 预览加载对应本地资源；目标小于 1MB，文字与 CTA 不烘焙进图片。
- 无优惠邀请和入群优惠券示例各自只有一个主要动作。待批方案在邮件外注明一次，VERSION DISABLED 保留在元数据或代码注释中，兑换入口不启用。平台政策冲突集中说明一次，不能以问卷答谢方案替代入群方案。公开预览不出现模板代码字段；操作说明使用普通语言。

### 品牌与周边企划

- 两条路线均为讨论用 reference 阅读页。首屏呈现标题、核心建议和方案要点，战略图接续正文，不使用全屏装饰首图，也不把建议写成已确认的会议决定。
- 复用系统字体、暖金强调和全局字号、间距；正文 16px、行高至少 1.65，不另设更大的标题上限。
- 内容采用 55% 深色遮罩，嵌套面板低透明度；连续阅读为主，仅真实对照内容可双栏，不用重复卡片堆砌。周边三个设计方向按“标题、图、必要说明”逐项纵向排列，每张图独占完整内容宽度，桌面也不并排缩成三张小图。
- 桌面流程可横向，窄屏改纵向，所有节点保留。概念图完整展示、无裁切，图注明确未生产。触控最小 44px，手机只保留单列阅读。
- 无新动画、无新依赖；现有菜单交互与 reduced-motion 保持。原生概念图留在本地，网页导出优先控制在每张 600KB 内。

## 响应式

- 固定验收宽度：390、768、1024、1440px。
- 触控目标最小 44×44px。
- 手机端保持标题、第一条有效内容和主要入口在首屏附近；表格改为清楚的键值行。
- 禁止横向滚动、被截断标题和窄到逐字换行的数据列。
- 980px 以下使用收起式导航及键值行表格，避免平板五列文本逐字换行。关闭侧栏时不参与键盘焦点；打开后焦点留在导航，Escape 或关闭按钮返回菜单按钮。

## 性能

- 常规页面共享 Hub 固定流体背景，不再加载其他全页装饰背景图。
- Hub 固定流体背景是已批准的体积例外；其他首屏业务图建议小于 600KB，非首屏图片使用 `loading="lazy"` 并写明宽高。
- 字体不通过 CSS `@import` 加载。
- 不为装饰引入 JavaScript、模糊滤镜或持续动画。

## 动效

- 交互反馈使用 160ms，导航使用 180ms，图片或内容显现最多 420ms。
- 动效需要解释状态、方向或连续性。装饰性漂浮、循环动画和滚动视差不进入工作档案。
- `prefers-reduced-motion: reduce` 下把过渡降到 0.01ms，并关闭平滑滚动。

## 发布前验收

- 文字：标题模板句式、重复副标题、解释性小字、模型自述和空泛结尾均清理。
- 代码：没有叙述型注释、隐藏 fallback、吞异常、重复 helper、无依据 TODO 和无必要抽象。
- 视觉：检查文字阴影、最小字号、触控尺寸、首屏密度、卡片必要性和设计 token；核对正式 Logo、全宽图片、应用页眉与问题对齐、进度两行结构，以及预览与真实应用一致。
- 浏览器：四个固定宽度均检查运行时错误、坏图、横向溢出和主要内容位置。
